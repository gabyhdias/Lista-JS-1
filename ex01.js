function somarNumeros() {
    let soma=0;
    let continuar = true;
    while <continuar) {
        let numero = parseFloat(prompt(Digite um número para somar: "));
            if (!isNaN(numero)) {
                soma += numero;
            }
    }
}